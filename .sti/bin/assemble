#!/bin/sh -x

echo "== Assemble ==="
getent ahosts frontend-xiuwang.router.default.svc.cluster.local || echo "Unknown host"
curl 'frontend-xiuwang.router.default.svc.cluster.local' | egrep Welcome
cat /etc/resolv.conf
